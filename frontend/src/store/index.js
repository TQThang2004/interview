/**
 * store/index.js
 * Placeholder cho state management tập trung (Redux, Zustand...).
 * Hiện tại app dùng React Context (AuthContext, ThemeContext).
 */

// Export rỗng, mở rộng khi cần
export {};
